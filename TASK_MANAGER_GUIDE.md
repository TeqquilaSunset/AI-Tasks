# Task Manager - Система управления задачами

## Обзор

Система управления задачами (Task Manager) - это Jira-подобная система для управления задачами проекта с интеграцией RAG для интеллектуальных рекомендаций.

## Функциональность

### Основные возможности

- **Полный CRUD для задач**: создание, просмотр, обновление, поиск
- **Умные рекомендации**: AI-рекомендации по приоритетам с учётом сроков и контекста проекта
- **RAG-интеграция**: рекомендации на основе документации проекта
- **Фильтрация**: по статусу, приоритету, исполнителю
- **Естественный язык**: создавайте задачи простым текстом, AI сам распарсит параметры
- **MCP инструменты**: доступ к задачам через MCP протокол

## Структура данных

Задачи хранятся в `data/tasks.json` в формате:

```json
{
  "projects": [...],
  "tasks": [
    {
      "id": "task_001",
      "project_id": "proj_001",
      "title": "Название задачи",
      "description": "Описание",
      "status": "todo",
      "priority": "high",
      "type": "feature",
      "assignee": "username",
      "labels": ["frontend", "urgent"],
      "created_at": "2026-01-15T08:00:00Z",
      "updated_at": "2026-01-15T09:30:00Z",
      "due_date": "2026-01-20T18:00:00Z",
      "estimated_hours": 8,
      "story_points": 5,
      "subtasks": [...],
      "comments": [...]
    }
  ],
  "sprints": [...],
  "users": [...]
}
```

## Команды чата

### Просмотр статуса

```
/task                      # Показать общий статус проекта
/task status               # То же самое
```

Вывод:
- Всего задач
- Выполнено (%)
- В работе
- К выполнению
- Просрочено
- Без исполнителя
- По приоритетам
- По типам

### Список задач

```
/task list                 # Все задачи
/task list high            # С высоким приоритетом
/task list todo            # Только к выполнению
/task list in progress     # В работе
```

### Создание задач

```
/task create исправить баг в авторизации
/task new Нужно добавить оптимизацию PDF pipeline
/task add Документация по MCP интеграции, приоритет high
```

AI автоматически определит:
- Приоритет (из описания)
- Тип (bug/feature/enhancement/etc)
- Описание
- Метки
- Оценку (story points)

### Рекомендации

```
/task recommend            # AI-рекомендации с RAG
/task priority            # То же самое
/task what to do          # Что делать сначала?
```

Покажет:
- Топ-3 задачи с объяснением почему
- Рекомендации из документации проекта
- Общие советы по приоритизации

### Просмотр задачи

```
/task get:task_001        # Подробная информация о задаче
```

### Поиск

```
/task поиск авторизация   # Поиск по названию/описанию/меткам
/task search RAG          # Задачи про RAG
```

### Обновление

```
/task update:task_001 изменить статус на in progress
/task update:task_001 приоритет critical
/task update:task_001 назначить на ivan
```

### Общие вопросы

```
/task какие задачи просрочены?
/task сколько задач в работе?
/task покажи задачи с меткой gpu
```

AI ответит, используя контекст всех задач + RAG документацию.

## MCP Инструменты

Доступны через MCP сервер:

### `task_create`
Создать новую задачу.

Параметры:
- `title` (строка): Название
- `description` (строка): Описание
- `priority` (строка): critical/high/medium/low
- `task_type` (строка): feature/bug/enhancement/documentation/optimization/refactoring
- `assignee` (строка): Исполнитель
- `labels` (строка): Метки через запятую
- `due_date` (строка): Срок в ISO формате
- `estimated_hours` (число): Оценка в часах
- `story_points` (число): Story Points

### `task_list`
Показать список задач с фильтрацией.

Параметры:
- `status` (строка): Фильтр по статусу
- `priority` (строка): Фильтр по приоритету
- `assignee` (строка): Фильтр по исполнителю
- `limit` (число): Макс. количество (по умолчанию 20)

### `task_get`
Получить подробную информацию о задаче.

Параметры:
- `task_id` (строка): ID задачи

### `task_update_status`
Обновить статус задачи.

Параметры:
- `task_id` (строка): ID задачи
- `status` (строка): Новый статус

### `task_update_priority`
Обновить приоритет задачи.

Параметры:
- `task_id` (строка): ID задачи
- `priority` (строка): Новый приоритет

### `task_assign`
Назначить задачу на пользователя.

Параметры:
- `task_id` (строка): ID задачи
- `assignee` (строка): Имя пользователя

### `task_add_comment`
Добавить комментарий к задаче.

Параметры:
- `task_id` (строка): ID задачи
- `author` (строка): Автор
- `content` (строка): Текст комментария

### `task_add_subtask`
Добавить подзадачу.

Параметры:
- `task_id` (строка): ID родительской задачи
- `title` (строка): Название подзадачи

### `task_search`
Поиск задач.

Параметры:
- `query` (строка): Поисковый запрос
- `limit` (число): Макс. результатов (по умолчанию 10)

### `task_recommend`
Получить рекомендации по приоритетам.

Параметры:
- `limit` (число): Количество рекомендаций (по умолчанию 5)
- `assignee` (строка): Фильтр по исполнителю

### `task_status`
Общий статус проекта и статистика.

Без параметров.

## Примеры использования

### Пример 1: Планирование спринта

```
Вы: /task recommend

AI: Рекомендации по приоритетам:

1. Исправить баг с timeout в Ollama
   - Приоритет: CRITICAL
   - Причина: Критический баг, срок менее 1 дня
   - Совет: Начни с этого, это блокирует других пользователей

2. Оптимизировать PDF pipeline для GPU
   - Приоритет: HIGH
   - Причина: Срок менее 3 дней, высокая ценность
   - Совет: После бага, это даст максимальное улучшение

3. Реализовать систему управления задачами
   - Приоритет: HIGH
   - Причина: Уже в работе, важно завершить
   - Совет: Продолжай работу над этой задачей

Общая рекомендация:
Начни с критического бага (30 мин), затем продолжи оптимизацию GPU (2-3 часа).
```

### Пример 2: Создание задачи через AI

```
Вы: /task create Нужно исправить проблему с авторизацией - пользователи не могут войти через Google

AI: Создана задача!

📌 Исправить авторизацию через Google
ID: task_007
Приоритет: HIGH
Тип: bug
Статус: todo
Оценка: 5 SP, 6 ч

Используй /task get:task_007 для просмотра деталей.
```

### Пример 3: Вопросы о задачах

```
Вы: /task какие у нас задачи по оптимизации?

AI: Задачи по оптимизации:

1. Оптимизировать PDF pipeline для GPU [in_progress]
   - Улучшить производительность обработки PDF за счет лучшей утилизации GPU
   - Оценка: 8 SP, 12 часов

2. (из документации) Рекомендации:
   - Используйте батчинг для эмбеддингов
   - Настройте OLLAMA_NUM_PARALLEL для вашего GPU
   - Профилируйте перед оптимизацией

Совет: Продолжайте работу над task_003, это приоритетная задача.
```

## Приоритеты

### Critical (🔴)
- Блокирующие проблемы
- Просроченные задачи
- Критические баги продакшена

### High (🟠)
- Важные функции
- Срок менее 3 дней
- Баги влияющие на пользователей

### Medium (🟡)
- Общие улучшения
- Срок менее недели
- Документация

### Low (🟢)
- Желательные улучшения
- Рефакторинг
- Оптимизации

## Статусы

- **todo** (📋): К выполнению
- **in_progress** (🔄): В работе
- **in_review**: На ревью
- **done** (✅): Выполнено
- **open** (📌): Открыто
- **cancelled**: Отменено

## Типы задач

- **feature**: Новая функциональность
- **bug**: Ошибка
- **enhancement**: Улучшение
- **documentation**: Документация
- **optimization**: Оптимизация
- **refactoring**: Рефакторинг

## Интеграция с RAG

Система автоматически использует RAG для:

1. **Рекомендаций**: Учитывает лучшие практики из документации
2. **Создания задач**: Определяет тип и приоритет на основе контекста
3. **Ответов на вопросы**: Дополняет ответы информацией из документации

Для работы RAG-интеграции необходимо:

```bash
# Индексировать документацию проекта
python index_docs.py
```

## Тестирование

Запустите тестовый скрипт:

```bash
python test_task_manager.py
```

## Архитектура

```
src/services/task_service.py  # Бизнес-логика
  ├── TaskService              # Основной класс
  ├── Task, Project, Sprint    # Модели данных
  └── TaskRecommendation       # Модель рекомендаций

data/tasks.json                # Хранилище данных

mcp_server.py                  # MCP инструменты
  ├── task_create
  ├── task_list
  ├── task_get
  ├── task_update_status
  ├── task_update_priority
  ├── task_assign
  ├── task_search
  ├── task_recommend
  └── task_status

main.py                        # CLI интеграция
  ├── /task command handlers
  ├── AI-powered task creation
  └── RAG-enhanced recommendations
```

## Советы по использованию

1. **Начните с рекомендаций**: `/task recommend` покажет что важно
2. **Создавайте задачи естественным языком**: AI сам определит параметры
3. **Используйте фильтры**: `/task list high priority` для важных задач
4. **Проверяйте статус**: `/task` для обзора проекта
5. **Задавайте вопросы**: `/task какие задачи просрочены?` для аналитики

## Примеры workflow

### Daily Standup
```
1. /task recommend           # Что делать сегодня?
2. /task update:task_001 начать работу
3. /task list in progress    # Показать текущие задачи
```

### Planning
```
1. /task status              # Обзор проекта
2. /task list todo           # Что нужно сделать?
3. /task create ...          # Создать новые задачи
```

### Review
```
1. /task list done           # Что завершено?
2. /task get:task_001        # Детали задачи
3. /task update:task_001 закрыть
```

## Будущие улучшения

- [ ] Web UI
- [ ] Интеграция с GitHub Issues
- [ ] Автоматическое создание задач из коммитов
- [ ] Time tracking
- [ ] Команды и роли
- [ ] Уведомления
- [ ] Дашборд с метриками
