# Support Chat System - Система поддержки с AI

## Обзор

Система поддержки - это интерактивное консольное приложение для агентов поддержки, которое использует:
- **RAG (Retrieval Augmented Generation)** для поиска по FAQ и документации
- **JSON-хранилище** для тикетов и пользователей
- **Автоопределение контекста** тикета из сообщения пользователя
- **AI-подсказки** для ответов на основе документации

## Возможности

### 1. Управление тикетами
- Просмотр всех тикетов
- Создание новых тикетов
- Просмотр детальной информации о тикете
- Поиск тикетов по ключевым словам
- Добавление сообщений в тикет

### 2. RAG-поиск по документации
- Автоматический поиск релевантных разделов FAQ
- Поиск по проектной документации
- Комбинированный контекст для AI-ответов
- Настройка порога релевантности

### 3. AI-ассистент поддержки
- Генерация ответов на основе документации
- Учет контекста тикета
- История переписки
- Категоризация вопросов

## Установка и запуск

### Предварительные требования

```bash
# 1. Запустите Qdrant (векторная база данных)
docker run -p 6333:6333 qdrant/qdrant

# 2. Запустите Ollama (для эмбеддингов)
ollama serve

# 3. Убедитесь, что документация проиндексирована
python index_docs.py
```

### Запуск системы поддержки

```bash
# Базовый запуск
python support_chat.py

# С кастомным параметрами
python support_chat.py --model glm-4.5-air --temp 0.7 --tickets data/tickets.json
```

## Команды чата

### Управление тикетами
- `tickets` - Список всех тикетов
- `ticket <id>` - Открыть тикет по ID
- `new` - Создать новый тикет
- `my <user_id>` - Установить текущего пользователя
- `search <query>` - Поиск тикетов

### Поиск информации
- `faq <query>` - Поиск по FAQ и документации
- `stats` - Статистика поддержки

### Управление чатом
- `help` / `?` - Справка
- `clear` - Очистить историю чата
- `quit` / `exit` - Выход

## Примеры использования

### Сценарий 1: Проблема с авторизацией

```
Вы: Почему не работает авторизация?

🔗 Обнаружен тикет: #ticket_001
[Детали тикета...]

🤖 Генерация ответа...

🎧 Ответ агента поддержки:
------------------------------------------------------------
Судя по вашему тикету, вы сталкиваетесь с ошибкой "Invalid credentials"
даже при вводе правильного пароля. Вот несколько рекомендаций:

1. **Проверьте токен авторизации**
   Токен мог истечь. Попробуйте получить новый токен:
   ```bash
   curl -X POST https://api.example.com/auth/refresh
   ```

2. **Проверьте переменные окружения**
   Убедитесь, что в файле `.env` указаны корректные значения:
   ```
   OPENAI_API_KEY=your_key_here
   OPENAI_BASE_URL=https://api.example.com/v1
   ```

3. **ПроверьтеCaps Lock и раскладку**
   Это частая причина проблем с паролем.

Если проблема сохраняется, пожалуйста, уточните:
- На каком именно этапе возникает ошибка?
- Какой интерфейс используете (CLI, API)?
------------------------------------------------------------

Добавить ответ в тикет? (y/n): y
✅ Ответ добавлен в тикет #ticket_001
```

### Сценарий 2: Вопрос по индексации PDF

```
Вы: Как проиндексировать большой PDF файл?

🤖 Генерация ответа...

🎧 Ответ агента поддержки:
------------------------------------------------------------
Для индексации больших PDF файлов (500+ страниц) рекомендую использовать
асинхронную версию пайплайна:

```bash
# Асинхронная версия (рекомендуется)
python pdf_pipeline_async.py --pdf_path document.pdf --max_concurrent 10

# GPU-оптимизированная версия
python run_pipeline_gpu.py --pdf_path doc.pdf --use_async --max_concurrent 15
```

**Параметры для больших файлов:**
- `--max_concurrent`: 10-15 параллельных запросов
- `--chunk_size`: 2048-4096 символов для ускорения

**Оптимизация для RTX 4070:**
```bash
set OLLAMA_NUM_PARALLEL=10
set OLLAMA_MAX_LOADED_MODELS=2
set OLLAMA_GPU_MEMORY=10240
```
------------------------------------------------------------
```

### Сценарий 3: Создание нового тикета

```
Вы: new

📝 Создание нового тикета
ID пользователя: user_002
Тема: Ollama timeout при RAG
Описание проблемы: При поиске через RAG получаю timeout ошибку
Приоритет (low/medium/high) [medium]: high

✅ Тикет создан: #ticket_005

[Детали нового тикета...]
```

## Структура данных

### Пользователи (Users)
```json
{
  "id": "user_001",
  "name": "Иван Петров",
  "email": "ivan.petrov@example.com",
  "company": "TechCorp",
  "tier": "premium",
  "created_at": "2024-01-15T10:00:00Z"
}
```

### Тикеты (Tickets)
```json
{
  "id": "ticket_001",
  "user_id": "user_001",
  "subject": "Не работает авторизация",
  "status": "open",
  "priority": "high",
  "category": "authentication",
  "created_at": "2024-03-10T08:30:00Z",
  "updated_at": "2024-03-10T09:15:00Z",
  "messages": [...]
}
```

## Архитектура

### Компоненты

1. **ticket_service.py** - Сервис управления тикетами
   - CRUD операции для тикетов и пользователей
   - Поиск и фильтрация
   - Автоопределение контекста

2. **faq_service.py** - RAG-сервис для FAQ
   - Поиск по документации
   - Категоризация вопросов
   - Генерация подсказок

3. **support_chat.py** - Основное приложение
   - Интерактивный CLI
   - AI-генерация ответов
   - Управление состоянием

### Поток данных

```
User Input
    ↓
Detect Ticket Context (ticket_service)
    ↓
Search FAQ/Documentation (faq_service + RAG)
    ↓
Generate AI Response (OpenAI)
    ↓
Add to Ticket (optional)
```

## Категории вопросов

Система автоматически категоризирует вопросы:
- `authentication` - авторизация, логин, пароль
- `configuration` - настройка, конфигурация
- `usage` - использование, как работать
- `technical` - ошибки, проблемы
- `pdf` - работа с PDF
- `rag` - RAG, поиск, эмбеддинги
- `mcp` - MCP инструменты
- `performance` - производительность
- `general` - общие вопросы

## Интеграция с существующими сервисами

### Использование RAG Service
```python
from src.services.faq_service import FAQService

faq = FAQService(collection_name="project_docs")
results = await faq.search_faq("Как настроить переменные окружения?")
```

### Использование Ticket Service
```python
from src.services.ticket_service import TicketService

tickets = TicketService("data/tickets.json")
ticket = tickets.create_ticket(
    user_id="user_001",
    subject="Новый вопрос",
    content="Описание проблемы",
    priority="high"
)
```

## Настройка

### Переменные окружения (.env)
```env
# OpenAI/LLM
OPENAI_API_KEY=your_key_here
OPENAI_BASE_URL=https://api.example.com/v1

# Опционально
OPENAI_VERIFY_SSL=false
```

### Конфигурация (src/config.py)
```python
DEFAULT_MODEL = "glm-4.5-air"
DEFAULT_TEMPERATURE = 0.7
DEFAULT_RELEVANCE_THRESHOLD = 0.1
DEFAULT_TOP_K = 5
```

## Тестовые данные

Файл `data/tickets.json` содержит:
- 3 тестовых пользователя
- 4 тестовых тикета с различными статусами
- Примеры сообщений

## Расширение функциональности

### Добавление новых полей в тикет
```python
# В ticket_service.py
def add_custom_field(ticket_id: str, field_name: str, value: Any):
    # Добавление кастомного поля
    pass
```

### Интеграция с реальной CRM
```python
# Замените JSON на API calls
class CrmTicketService(TicketService):
    def _load_data(self):
        # Загрузка из CRM API
        pass
```

### Добавление MCP инструментов
```python
# В support_chat.py
async def _call_mcp_tool(self, tool_name: str, params: dict):
    # Вызов MCP инструмента
    pass
```

## Troubleshooting

### Ошибка "Collection not found"
```bash
# Проиндексируйте документацию
python index_docs.py
```

### Ошибка "Ollama timeout"
```bash
# Проверьте, что Ollama запущен
ollama list

# Проверьте URL
curl http://localhost:11434/api/tags
```

### Тикеты не сохраняются
```bash
# Проверьте права на запись
ls -la data/

# Создайте директорию
mkdir -p data
```

## Следующие улучшения

- [ ] Web-интерфейс вместо CLI
- [ ] Интеграция с реальной CRM (Jira, Zendesk)
- [ ] Отправка email уведомлений
- [ ] Аналитика и метрики
- [ ] Мультиязычная поддержка
- [ ] Вебхуки для интеграций
- [ ] Экспорт отчетов

## Лицензия

MIT License - см. LICENSE файл
